global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
        labels:
          app: "prometheus"
  - job_name: springboot
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - chat-app-1:8080